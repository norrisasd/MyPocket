<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mypocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPocket">
        <activity android:name=".billings_edit_transaction" />
        <activity android:name=".add_bills" />
        <activity android:name=".savings_edit_transaction" />
        <activity android:name=".expense_edit_transaction" />
        <activity android:name=".income_edit_transaction" />
        <activity android:name=".Categories" />
        <activity android:name=".Transaction_details" />
        <activity android:name=".Billings" />
        <activity android:name=".Transaction" />
        <activity android:name=".Savings" />
        <activity android:name=".Add" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MyPocket.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MyPocket.NoActionBar" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>